apiVersion: v1
kind: Service
metadata:
  name: my-dashboard-lb
  namespace: my-dashboard
spec:
  type: LoadBalancer
  selector:
    app: my-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080

